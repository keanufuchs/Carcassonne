/**
 * Re-exports shared segment-highlight helpers used by the inline SVG board.
 * See `src/shared/segmentHighlight.ts` for the canonical definitions.
 */
export {
  SEGMENT_HIGHLIGHT,
  segmentElementId,
  parseSegmentLocalId,
  segmentClasses,
  highlightLocalIdsForSegment,
} from '../../shared/segmentHighlight';
